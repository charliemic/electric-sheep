name: Security Linting

on:
  push:
    branches: ['**']
    paths:
      - 'app/**'
      - 'build.gradle*'
  pull_request:
    branches: ['**']
    paths:
      - 'app/**'
      - 'build.gradle*'

permissions:
  contents: read
  pull-requests: write

jobs:
  security-lint:
    name: Security-Focused Lint Checks
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'
      
      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          accept-android-sdk-licenses: true
      
      - name: Cache Gradle dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      
      - name: Run security-focused lint
        run: |
          ./gradlew lint --stacktrace \
            -Pandroid.lint.checkAllWarnings=true \
            -Pandroid.lint.checkSecurity=true \
            -Pandroid.lint.checkHardcodedValues=true \
            -Pandroid.lint.checkInsecureRandom=true \
            -Pandroid.lint.checkSSLVulnerability=true \
            -Pandroid.lint.checkWeakCryptography=true
        continue-on-error: true
      
      - name: Upload lint results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-lint-results
          path: app/build/reports/lint-results*.html
          retention-days: 7
      
      - name: Parse lint results and comment PR
        if: github.event_name == 'pull_request' && always() && failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '## 🔒 Security Lint Results\n\n⚠️ **Security linting issues detected**\n\nPlease review the lint report in workflow artifacts and fix security issues before merging.\n\n**Common issues:**\n- Hardcoded values (secrets, API keys)\n- Insecure random number generation\n- SSL/TLS vulnerabilities\n- Weak cryptography\n\n📊 Full lint report available in workflow artifacts.\n🔍 Review and fix security issues before merging.'
            });
      
      - name: Fail if security issues found
        if: failure()
        run: |
          echo "❌ Security linting issues detected"
          echo "Please review the lint report and fix security issues."
          exit 1
