# Reusable workflow step template for GitHub CLI commands
# 
# Usage:
#   - name: Run GitHub CLI Command
#     uses: ./.github/workflows/templates/gh-cli-step.yml
#     with:
#       command: "gh issue view 123"
#
# Or inline:
#   - name: Run GitHub CLI Command
#     env:
#       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#     run: |
#       gh <command>

# Note: This is a template file for reference
# Actual implementation uses inline env: GITHUB_TOKEN in workflow steps

# Example usage in workflow:
#   - name: Update Issue Labels
#     env:
#       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#     run: |
#       gh issue edit "$issue" \
#         --remove-label "status:in-progress" \
#         --add-label "status:review"

