# Android SDK location
sdk.dir=/path/to/android/sdk

# Supabase Configuration (Production)
# These values are read by build.gradle.kts and exposed via BuildConfig
supabase.url=https://your-project.supabase.co
supabase.anon.key=your-anon-key

# Supabase Staging Configuration (Optional, for testing)
# Only used when USE_STAGING_SUPABASE is set to true in debug builds
# supabase.staging.url=https://your-staging-project.supabase.co
# supabase.staging.anon.key=your-staging-anon-key
